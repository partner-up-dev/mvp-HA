# Claim Realization Matrix

| PRD claim | Realization focus | Backend realization | Frontend realization | Coordination note |
| --- | --- | --- | --- | --- |
| A one-sentence intent can become a joinable collaboration object | creation and persistence | natural-language and structured PR creation APIs, draft/publish lifecycle, persisted PR state | landing entrypoints, create page, create flows, publish orchestration | backend owns created object semantics; frontend owns entry UX |
| One PR evolves through context-specific entry and trust flows | context-specific collaboration surfaces on top of one durable PR model | single PR lifecycle and persistence, event-scoped PR creation and discovery, canonical `/pr/*` contracts | single PR domain UI with context-aware event entry, detail, and reliability rendering | one PR vocabulary, context-specific flows, no PR-type split |
| Collaboration supports distribution, revisit, and re-entry | share and re-entry loop | canonical routes, detail-payload share metadata, share/meta endpoints, public config | route-scoped share orchestration, share UI, route navigation, history lists, page-level re-entry | backend owns canonical entity share metadata; frontend owns active share session and replay |
| Collaboration needs a minimum trust loop | participant reliability loop | confirmation rules, slot release, notifications, check-in, reliability persistence | CTA states, reminder subscription UI, check-in UI, explanation rendering | backend decides eligibility; frontend renders state and actions |
| Collaboration needs a minimum trust loop | admin booking fulfillment audit loop | booking-trigger detection, execution persistence, merged audit timeline, result-notification delivery | admin execution workspace, result submission, release action, audit rendering | booking execution is admin-facing product behavior realized through a shared admin contract plus backend-owned state |
| Identity supports collaboration instead of blocking it up front | lightweight-to-stronger identity path | anonymous/local/authenticated flows, OAuth/bind, token rotation | auth bootstrap, local storage, OAuth redirects, pending action resume | mixed token + cookie contract must stay coherent |
