# Claim Realization Matrix

| PRD claim | Backend realization | Frontend realization | Coordination note |
| --- | --- | --- | --- |
| A one-sentence intent can become a joinable collaboration object | natural-language and structured PR creation APIs, draft/publish lifecycle, persisted PR state | landing entrypoints, create page, create flows, publish orchestration | backend owns created object semantics; frontend owns entry UX |
| Community and Anchor collaboration evolve by scenario | separate route families, scene-specific use cases/controllers, and event-scoped Anchor PR creation | separate pages and domain UI for `/cpr/*`, `/apr/*`, and event-scoped create actions from `/events/:eventId` | shared base types, scene-specific contracts, no standalone generic Anchor create route |
| Collaboration supports distribution, revisit, and re-entry | canonical routes, detail-payload share metadata, share/meta endpoints, public config | route-scoped share orchestration, share UI, route navigation, history lists, page-level re-entry | backend owns canonical entity share metadata; frontend owns active share session and replay |
| Collaboration needs a minimum trust loop | confirmation rules, slot release, notifications, check-in, reliability persistence | CTA states, reminder subscription UI, check-in UI, explanation rendering | backend decides eligibility; frontend renders state and actions |
| Anchor booking fulfillment needs auditable return flow | booking-trigger detection, execution persistence, merged audit timeline, result-notification delivery | admin execution workspace, result submission, release action, audit rendering | booking execution is admin-facing product behavior realized through a shared admin contract plus backend-owned state |
| Identity supports collaboration instead of blocking it up front | anonymous/local/authenticated flows, OAuth/bind, token rotation | auth bootstrap, local storage, OAuth redirects, pending action resume | mixed token + cookie contract must stay coherent |
