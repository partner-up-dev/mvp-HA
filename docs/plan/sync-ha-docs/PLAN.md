# Plan: Sync H-A MVP Docs

## Goal

Align product docs and AGENTS.md with the H-A MVP key decisions in `docs/h_a_mvp_key_decisions.md`.

## Steps

1. Extract key decisions and boundaries from `docs/h_a_mvp_key_decisions.md` and list the doc deltas.
2. Update product docs:
   - `docs/product/overview.md` to reflect H-A definition, product positioning, and usage paths.
   - `docs/product/features/*.md` to match H-A scope and current MVP behaviors.
   - `docs/product/glossary.md` to align terminology (PartnerRequest, PartnerRequestPreset, statuses, etc.).
3. Update repo-level `AGENTS.md` files (root, backend, frontend) to reflect current MVP scope, sharing channels, and core definitions.
4. Note any remaining mismatches or follow-ups in `docs/plan/sync-ha-docs/RESULT.md`.

## Non-Goals

- No code changes.
- No behavior changes.

## Approval Needed

Proceed with the updates after you approve this plan.
