# Result

## Summary

- Added WeCom self-built app webhook at `/api/wecom/message` with encrypted callback verification, message decryption, and URL verification handling.
- Implemented async WeCom reply that creates PartnerRequest from text, generates a 4-digit PIN, and sends back the share link.
- Added WeCom crypto utility, API service, and new feature documentation.

## Tests

- Not run (not requested).
